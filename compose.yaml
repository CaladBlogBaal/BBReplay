services:
  api:
    build:
      dockerfile: Dockerfile
      context: .
    ports:
      - "5000:5000"
    volumes:
      - upload-files:/app/upload-files  # Docker volume for persistent storag
    networks:
      - my_network
    restart: "always"

volumes:
      - upload-files:/app/upload-files

networks:
  my_network: