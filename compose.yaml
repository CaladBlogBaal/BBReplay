services:
  api:
    build:
      dockerfile: Dockerfile
      context: .
    ports:
      - "5000:5000"
    volumes:
      - ./upload-files:/app/upload-files  # local directory for persistent storage
    networks:
      - my_network
    restart: "always"

networks:
  my_network: